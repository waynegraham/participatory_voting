<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= content_for?(:title) ? yield(:title) : "Participatory Voting" %></title>
        <meta name="description" content="<%= content_for?(:description) ? yield(:description) : " Community Voting Platform" %>">
        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
        <%= csrf_meta_tags %>
        <script>
          (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
          (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
          m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
          })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
          ga('create', 'UA-9461551-3', 'auto');
          ga('send', 'pageview');
        </script>
    </head>
    <body>
        <header>
            <%= render 'layouts/login'%>
        </header>

        <main>
            <div id="top"></div>
            <%= render 'layouts/nav_section' %>
            <%= render 'layouts/messages' %>
            <%= yield %>
        </main>

        <footer class="section bg-gray">
            <%= render 'layouts/footer' %>
        </footer>
        <button class="to-top">
            <span class="glyphicon glyphicon-chevron-up"></span>
        </button>
    </body>
</html>
